# Welcome to Input | Component

This Component receive 10 Props with multiple types (showPrefix, prefix, showSuffix, suffix, label,
customPlaceholder, customName, error, value, onChange) the prop label customName is mandatory.

This component can be easyly adapted to show a custom prefix or sufix.

It is prepared to showing a error message manipulating a error props.
